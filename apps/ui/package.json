{
  "name": "@anfpes/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"TODO: build UI\"",
    "dev": "echo \"TODO: dev UI\"",
    "lint": "eslint src --ext .ts,.tsx --no-error-on-unmatched-pattern",
    "test": "vitest run"
  }
}
